Deploy the planutils docker image.

1 - Run the docker image: 
    sudo docker build -t planutils_image .

2 - Run the container:
    sudo docker run -it --privileged planutils_image bash

3 - Inside the container (after executing step 2) copy the id

    e.g. root@e8122184f50e:~# -> e8122184f50e

    Otherwise, in other window use:

    docker container ls

    And copy the container id that corresponds to the image used (planutils_image). 

4 - Paste the id in the field CONTAINER_NAME (first cell) of both:
    ./notebook.ipynb
    ./pdd_tester/pddl_tester.ipynb

The notebooks can be used.

 - Installare planutils container per la PRIMA VOLTA con mount della directory corrente:
docker run -it --privileged --name my_planutils_container -v $(pwd):/work aiplanning/planutils:latest bash

 -  Avviare container le volte sucessive

 docker run -it --privileged --name my_planutils_container -v $(pwd):/work my_custom_planutils_image bash


 - Stop container
docker stop my_planutils_container

 - Start container and attach to terminal, saved container.
docker start -ai my_planutils_container

 - Salvare stato container dopo stop
 docker commit my_planutils_container my_custom_planutils_image